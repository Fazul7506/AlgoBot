# Recovery Engine

Recovery Engine records and dispatches service restarts, broker reconnects, order retries, queue recovery, and interrupted workflow restoration.

## Enterprise Scope

Implemented as modular Django services, API endpoints, templates, websocket event constants, and Celery-compatible tasks.
