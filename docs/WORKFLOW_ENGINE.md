# Workflow Engine

Workflow Engine executes persisted workflow nodes with auditing, approval pauses, and retry-ready execution records.

## Enterprise Scope

Implemented as modular Django services, API endpoints, templates, websocket event constants, and Celery-compatible tasks.
