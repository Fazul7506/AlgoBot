# Webhooks

Webhook notifications support outbound JSON payloads, custom headers, authentication, HMAC signatures, and retry metadata.

## Enterprise Scope

Implemented as modular Django services, API endpoints, templates, websocket event constants, and Celery-compatible tasks.
