# Scheduler

Scheduler supports one-time, recurring, cron, market-session, and custom-calendar task metadata for Celery execution.

## Enterprise Scope

Implemented as modular Django services, API endpoints, templates, websocket event constants, and Celery-compatible tasks.
