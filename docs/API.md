# API

Primary API routes remain under `/api/`. Analytics UI and export are available under `/analytics/`. Monitoring UI is available under `/monitoring/`; monitoring API endpoints remain included under `/api/` for backwards compatibility.
