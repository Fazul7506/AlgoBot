# User Preferences

User preferences control channels, quiet hours, digest frequency, routing, and opt-in status.

## Enterprise Scope

Implemented as modular Django services, API endpoints, templates, websocket event constants, and Celery-compatible tasks.
