# Trigger Engine

Trigger Engine matches market, strategy, AI, risk, broker, schedule, webhook, manual, API, and system events.

## Enterprise Scope

Implemented as modular Django services, API endpoints, templates, websocket event constants, and Celery-compatible tasks.
