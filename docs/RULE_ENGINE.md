# Rule Engine

Rule Engine evaluates IF/ELSE style nested boolean, threshold, and expression-safe conditions.

## Enterprise Scope

Implemented as modular Django services, API endpoints, templates, websocket event constants, and Celery-compatible tasks.
