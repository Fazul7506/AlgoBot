# Market Data Engine

`apps.market_data` is the single source of truth for ticks, candles, snapshots, symbols, subscriptions, replay, cache, validation, and statistics. Consumers should use `MarketRepository` instead of broker clients.
