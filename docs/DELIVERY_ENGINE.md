# Delivery Engine

Delivery Engine tracks queued, sending, sent, delivered, opened, clicked, failed, retried, and cancelled states.

## Enterprise Scope

Implemented as modular Django services, API endpoints, templates, websocket event constants, and Celery-compatible tasks.
