/* AlgoBot Unified Service Runtime. Cross-service browser infrastructure lives here. */
(() => {
  'use strict';
  if (window.AlgoBotServiceRuntime) return;
  const active=new Map(),controllers=new Map(),registry=new Map(),errors=[],recentErrorKeys=new Map();
  const MAX_ERRORS=100,now=()=>new Date().toISOString();
  const accountId=()=>String(window.AlgoBotAccountContext?.getSelectedId?.()||window.AlgoBotBrokerState?.get?.()?.account?.id||'');
  const emit=(name,detail={})=>window.dispatchEvent(new CustomEvent(name,{detail:{...detail,at:now(),accountId:accountId()}}));
  const inferService=url=>{const path=String(url||'').toLowerCase();if(path.includes('/ai/')||path.includes('/predict'))return'ai';if(path.includes('data-center')||path.includes('/market'))return'market-data';if(path.includes('/orders'))return'orders';if(path.includes('/positions'))return'positions';if(path.includes('/strateg'))return'strategies';if(path.includes('/risk'))return'risk';if(path.includes('/monitor'))return'monitoring';if(path.includes('/notification'))return'notifications';if(path.includes('/automation'))return'automation';if(path.includes('/portfolio'))return'portfolio';if(path.includes('/backtest'))return'backtesting';return document.body?.dataset.service||'platform'};
  const register=(name,config={})=>{if(!name)throw new Error('A service name is required.');const value={...(registry.get(name)||{}),...config,name,registeredAt:(registry.get(name)||{}).registeredAt||now()};registry.set(name,value);emit('algobot:service-registered',{service:value});return Object.freeze({...value})};
  const begin=(name,label='Loading…')=>{active.set(name,(active.get(name)||0)+1);document.documentElement.dataset.algobotBusy='true';emit('algobot:service-loading',{service:name,label,count:active.get(name)});return()=>end(name)};
  const end=name=>{const count=Math.max(0,(active.get(name)||0)-1);count?active.set(name,count):active.delete(name);if(!active.size)delete document.documentElement.dataset.algobotBusy;emit('algobot:service-idle',{service:name,count})};
  const retryable=e=>Boolean(e&&(e.retryable??(['NETWORK_ERROR','API_TIMEOUT','SERVICE_TIMEOUT'].includes(e.code)||Number(e.status)>=500)));
  async function withTimeout(task,timeout=25000,name='service'){const key=`${name}:${Date.now()}:${Math.random()}`,controller=new AbortController();controllers.set(key,controller);let timer;try{return await Promise.race([Promise.resolve().then(()=>task({signal:controller.signal})),new Promise((_,reject)=>{timer=setTimeout(()=>{controller.abort();const e=new Error(`${name} timed out after ${timeout}ms.`);e.code='SERVICE_TIMEOUT';e.service=name;e.retryable=true;reject(e)},Math.max(1000,timeout))})])}finally{clearTimeout(timer);controllers.delete(key)}}
  async function run(name,task,{timeout=25000,retry=0,retryDelay=1000,label='Loading…'}={}){const release=begin(name,label);try{for(let attempt=0;attempt<=retry;attempt++){try{return await withTimeout(task,timeout,name)}catch(e){if(attempt>=retry||!retryable(e))throw e;await new Promise(r=>setTimeout(r,retryDelay*(attempt+1)))}}}finally{release()}}
  const cancel=name=>{for(const[key,c]of controllers)if(key.startsWith(`${name}:`))c.abort();emit('algobot:service-cancelled',{service:name})};
  const recordError=detail=>{const e={at:now(),service:detail.service||inferService(detail.url),url:detail.url||'',method:detail.method||'GET',status:Number(detail.status||0),code:detail.code||'UNKNOWN_ERROR',message:detail.message||'Request failed.',accountId:accountId(),retryable:Boolean(detail.retryable)};const key=`${e.service}|${e.url}|${e.method}|${e.status}|${e.code}|${e.accountId}|${e.message}`,last=recentErrorKeys.get(key)||0;if(Date.now()-last<1000)return{...e,deduped:true};recentErrorKeys.set(key,Date.now());errors.push(e);if(errors.length>MAX_ERRORS)errors.shift();emit('algobot:service-error',{error:e});return e};
  const request=async(name,url,options={},timeout=25000)=>{const service=name||inferService(url),headers=new Headers(options.headers||{}),id=accountId();headers.set('Accept',headers.get('Accept')||'application/json');if(id&&!headers.has('X-Algobot-Account-ID'))headers.set('X-Algobot-Account-ID',id);try{const response=await withTimeout(({signal})=>fetch(url,{credentials:'same-origin',cache:'no-store',...options,headers,signal}),timeout,service),text=await response.text();let payload={};try{payload=text?JSON.parse(text):{}}catch(_){payload={detail:text}}if(!response.ok){const error=Object.assign(new Error(payload?.detail||payload?.message||`${service} request failed (${response.status}).`),{status:response.status,payload,code:`HTTP_${response.status}`,retryable:response.status>=500||response.status===429});recordError({service,url,method:options.method||'GET',status:error.status,code:error.code,message:error.message,retryable:error.retryable});throw error}return payload}catch(error){if(error?.code!=='SERVICE_TIMEOUT'&&!error?.code?.startsWith?.('HTTP_'))recordError({service,url,method:options.method||'GET',status:error?.status||0,code:error?.code||'NETWORK_ERROR',message:error?.message||'Network request failed.',retryable:true});throw error}};
  const snapshot=()=>({accountId:accountId(),services:[...registry.values()].map(v=>({...v})),loading:Object.fromEntries(active),errors:errors.slice(-20)});
  window.AlgoBotServiceRuntime=Object.freeze({register,begin,end,run,withTimeout,request,cancel,isRetryable:retryable,recordError,recentErrors:n=>errors.slice(-Math.max(1,n||20)),snapshot,accountId});
  window.addEventListener('algobot:api-error',event=>{const entry=recordError(event.detail||{});if(!entry.deduped)window.dispatchEvent(new CustomEvent('algobot:recoverable-error',{detail:entry}))});
  const reset=()=>{active.clear();delete document.documentElement.dataset.algobotBusy;emit('algobot:services-account-reset')};
  window.addEventListener('algobot:account-changed',reset);window.addEventListener('algobot:account-context-changed',reset);
  window.addEventListener('algobot:recoverable-error',event=>{const d=event.detail||{};document.querySelectorAll('[data-global-error]').forEach(el=>{el.textContent=d.message||'The service is temporarily unavailable. Please try again.';el.hidden=false});document.querySelectorAll('[data-service-retry]').forEach(el=>{el.hidden=!d.retryable;el.dataset.retryService=d.service||''})});
  // Compatibility boundary: legacy page scripts may still call fetch directly;
  // same-origin calls inherit account context and centralized error classification.
  const featureFetch=window.fetch.bind(window);
  window.fetch=async(input,init={})=>{const url=typeof input==='string'?input:input?.url||'',headers=new Headers((typeof input==='object'&&input?.headers)||{});new Headers(init.headers||{}).forEach((v,k)=>headers.set(k,v));const id=accountId();try{const target=new URL(url,window.location.origin);if(target.origin===window.location.origin&&id&&!headers.has('X-Algobot-Account-ID'))headers.set('X-Algobot-Account-ID',id);const response=await featureFetch(input,{...init,headers});if(!response.ok)recordError({service:inferService(url),url,method:init.method||'GET',status:response.status,code:`HTTP_${response.status}`,message:`${inferService(url)} request failed (${response.status}).`,retryable:response.status>=500||response.status===429});return response}catch(error){if(!['API_TIMEOUT','REQUEST_ABORTED'].includes(error?.code))recordError({service:inferService(url),url,method:init.method||'GET',status:error?.status||0,code:error?.code||'NETWORK_ERROR',message:error?.message||'Network request failed.',retryable:true});throw error}};
  emit('algobot:service-runtime-ready',{version:5});
})();
