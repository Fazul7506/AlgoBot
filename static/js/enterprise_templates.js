(function(){
  function draw(canvas){const dpr=window.devicePixelRatio||1,box=canvas.getBoundingClientRect();canvas.width=box.width*dpr;canvas.height=box.height*dpr;const c=canvas.getContext('2d');c.scale(dpr,dpr);const w=box.width,h=box.height;c.clearRect(0,0,w,h);c.strokeStyle='rgba(143,164,194,.18)';c.lineWidth=1;for(let i=1;i<6;i++){c.beginPath();c.moveTo(0,h*i/6);c.lineTo(w,h*i/6);c.stroke()}const grad=c.createLinearGradient(0,0,w,0);grad.addColorStop(0,'#29d3ff');grad.addColorStop(1,'#33d69f');c.strokeStyle=grad;c.lineWidth=3;c.beginPath();for(let x=0;x<w;x+=8){const y=h*.55+Math.sin(x/38)*34+Math.cos(x/91)*22-x/w*60;if(x===0)c.moveTo(x,y);else c.lineTo(x,y)}c.stroke();c.fillStyle='rgba(41,211,255,.08)';c.lineTo(w,h);c.lineTo(0,h);c.fill()}
  function init(){document.querySelectorAll('[data-enterprise-chart]').forEach(draw);document.querySelectorAll('[data-table-search]').forEach(input=>input.addEventListener('input',()=>{const table=input.closest('.data-panel').querySelector('tbody');[...table.rows].forEach(r=>{r.hidden=!r.textContent.toLowerCase().includes(input.value.toLowerCase())})}));document.querySelectorAll('[data-action="refresh"]').forEach(b=>b.addEventListener('click',()=>{b.textContent='Refreshing…';setTimeout(()=>b.textContent='Refresh',700)}));document.querySelector('[data-theme-toggle]')?.addEventListener('click',()=>document.documentElement.dataset.theme=document.documentElement.dataset.theme==='dark'?'light':'dark')}
  addEventListener('resize',()=>document.querySelectorAll('[data-enterprise-chart]').forEach(draw));document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init):init();
})();
