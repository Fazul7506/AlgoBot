(() => {
  const $=(selector,root=document)=>root.querySelector(selector);
  let selectedSymbol=$('[data-symbol]')?.value||''; let livePoints=[]; let busy=false; let lastAccounts=[]; let accountRequestInFlight=false; let brokerSyncInFlight=false;
  const csrf=()=>document.cookie.match(/(?:^|;\s*)csrftoken=([^;]+)/)?.[1]||'';
  const request=async(url,options={},timeoutMs=7000)=>{const headers={Accept:'application/json',...(options.headers||{})};if(options.method&&options.method!=='GET')headers['X-CSRFToken']=csrf();const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),timeoutMs);try{const r=await fetch(url,{credentials:'same-origin',...options,headers,signal:controller.signal});const text=await r.text();let data={};try{data=text?JSON.parse(text):{};}catch(_){data={detail:text};}if(r.status===401||r.status===403)throw new Error('Your AlgoBot session has expired. Reconnect the broker.');if(!r.ok)throw new Error(data.detail||data.message||`Request failed (${r.status})`);return data;}catch(e){if(e.name==='AbortError')throw new Error('Request timed out');throw e;}finally{clearTimeout(timer);}};
  const list=v=>Array.isArray(v)?v:(Array.isArray(v?.results)?v.results:(Array.isArray(v?.data)?v.data:[]));
  const fmt=v=>v==null||v===''||Number.isNaN(Number(v))?'—':Number(v).toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:8});
  const safe=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
  function setGlobalConnection(text,state='neutral'){const node=$('[data-global-connection]');if(!node)return;node.classList.toggle('connected',state==='connected');node.classList.toggle('error',state==='error');node.innerHTML=`<i></i><span>${safe(text)}</span>`;node.title=text;}
  function setStatus(text,bad=false){const node=$('#terminal-status');if(!node)return;node.innerHTML=`<span class="status-dot${bad?' is-bad':''}"></span>${safe(text)}`;}
  function renderAccounts(accounts){lastAccounts=accounts.filter(a=>a&&a.id&&a.broker_account_id);const select=$('#account');if(!select)return;if(!lastAccounts.length){select.innerHTML='<option value="">No connected broker account</option>';setGlobalConnection('No broker connected','error');return;}const current=select.value;select.innerHTML=lastAccounts.map(a=>`<option value="${a.id}">${safe(a.broker?.name||'Broker')} · ${safe(a.broker_account_id)} · ${safe(String(a.account_type||'demo').toUpperCase())} · ${safe(a.currency||'')} ${fmt(a.balance)}</option>`).join('');const preferred=lastAccounts.find(a=>a.is_default||a.is_preferred)||lastAccounts[0];select.value=lastAccounts.some(a=>String(a.id)===current)?current:String(preferred.id);updateAccountCards();}
  function updateAccountCards(){const account=lastAccounts.find(a=>String(a.id)===String($('#account')?.value))||lastAccounts.find(a=>a.is_default||a.is_preferred)||lastAccounts[0];if(!account){setGlobalConnection('No broker connected','error');return;}const type=String(account.account_type||'demo').toUpperCase();const label=`${account.broker?.name||'Broker'} · ${account.broker_account_id} · ${type}`;const status=$('#terminal-status');if(status&&!busy)status.innerHTML=`<span class="status-dot"></span>${safe(label)}`;const balance=$('#balance'),equity=$('#equity'),margin=$('#margin');if(balance)balance.textContent=`${account.currency||''} ${fmt(account.balance)}`.trim();if(equity)equity.textContent=`${account.currency||''} ${fmt(account.equity??account.balance)}`.trim();if(margin)margin.textContent=`${account.currency||''} ${fmt(account.margin)}`.trim();const kpi=$('[data-kpi="balance"]');if(kpi)kpi.textContent=`${account.currency||''} ${fmt(account.balance)}`.trim();const accountLabel=$('[data-terminal-account]');if(accountLabel)accountLabel.textContent=`Account: ${account.broker_account_id}`;setGlobalConnection(label,'connected');}
  async function syncSelectedAccount(){if(brokerSyncInFlight)return;const account=lastAccounts.find(a=>String(a.id)===String($('#account')?.value))||lastAccounts.find(a=>a.is_default||a.is_preferred)||lastAccounts[0];if(!account)return;brokerSyncInFlight=true;try{const result=await request(`/api/brokers/accounts/${account.id}/sync/`,{method:'POST'},8000);if(result.account){const index=lastAccounts.findIndex(a=>a.id===result.account.id);if(index>=0)lastAccounts[index]=result.account;renderAccounts(lastAccounts);setGlobalConnection(`${result.account.broker?.name||'Broker'} · ${result.account.broker_account_id} · ${String(result.account.account_type||'demo').toUpperCase()}`,'connected');}}catch(error){setGlobalConnection(`${account.broker?.name||'Broker'} · ${account.broker_account_id} · sync delayed`,'connected');setStatus(`Broker sync delayed: ${error.message}`,true);}finally{brokerSyncInFlight=false;}}
  async function syncAccounts(){if(accountRequestInFlight)return lastAccounts;accountRequestInFlight=true;try{const accounts=list(await request('/api/brokers/accounts/',{},4500));renderAccounts(accounts);if(accounts.length){const account=accounts.find(a=>a.is_default||a.is_preferred)||accounts[0];setGlobalConnection(`${account.broker?.name||'Broker'} · ${account.broker_account_id} · ${String(account.account_type||'demo').toUpperCase()}`,'connected');setTimeout(syncSelectedAccount,300);}return accounts;}catch(error){setGlobalConnection(`Broker status unavailable · ${error.message}`,'error');setStatus(`Broker account list unavailable: ${error.message}`,true);return lastAccounts;}finally{accountRequestInFlight=false;}}
  async function discoverSymbol(){if(selectedSymbol)return selectedSymbol;try{const symbols=list(await request('/api/markets/symbols/',{},4500));const first=symbols.find(x=>x?.symbol&&x.is_active!==false&&x.is_tradable!==false);selectedSymbol=first?.symbol||'';const select=$('[data-symbol]');if(select){select.innerHTML=symbols.filter(x=>x?.symbol&&x.is_active!==false&&x.is_tradable!==false).map(x=>`<option value="${safe(x.symbol)}">${safe(x.display_name||x.symbol)}</option>`).join('');if(selectedSymbol)select.value=selectedSymbol;}return selectedSymbol;}catch(_){setStatus('Market catalogue unavailable',true);return '';}}
  function renderLiveChart(){const chart=$('#chart');if(!chart||livePoints.length<2)return;const width=1000,height=330,pad=18,values=livePoints.map(p=>p.price),min=Math.min(...values),max=Math.max(...values),span=max-min||Math.max(Math.abs(max)*.0001,1),points=livePoints.map((p,i)=>`${(pad+(i/Math.max(1,livePoints.length-1))*(width-pad*2)).toFixed(1)},${(height-pad-((p.price-min)/span)*(height-pad*2)).toFixed(1)}`).join(' '),latest=values.at(-1),stroke=latest>=values[0]?'#43d19a':'#ff6b7d',last=points.split(' ').at(-1).split(',');chart.innerHTML=`<svg viewBox="0 0 ${width} ${height}" preserveAspectRatio="none" role="img" aria-label="Live market price chart" style="width:100%;height:100%;display:block"><polyline points="${points}" fill="none" stroke="${stroke}" stroke-width="2.5" stroke-linejoin="round" stroke-linecap="round"></polyline><circle cx="${last[0]}" cy="${last[1]}" r="4" fill="${stroke}"></circle><text x="${width-pad}" y="${pad+2}" text-anchor="end" fill="currentColor" opacity=".7" font-size="13">LIVE ${fmt(latest)}</text></svg>`;}
  async function syncLiveTick(){const symbol=$('[data-symbol]')?.value||selectedSymbol||await discoverSymbol();if(!symbol||busy)return;selectedSymbol=symbol;busy=true;try{const tick=await request('/api/market/ticks/broker/',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({symbol})},5000);const price=Number(tick.quote??tick.last??tick.price);if(!Number.isFinite(price))throw new Error('Broker returned no usable quote');livePoints.push({epoch:Number(tick.epoch||Date.now()/1000),price});livePoints=livePoints.slice(-120);renderLiveChart();$('[data-q="price"]')?.replaceChildren(document.createTextNode(fmt(price)));$('[data-q="bid"]')?.replaceChildren(document.createTextNode(fmt(tick.bid??price)));$('[data-q="ask"]')?.replaceChildren(document.createTextNode(fmt(tick.ask??price)));const title=$('#chart-title');if(title)title.textContent=`${symbol} · ${$('[data-timeframe]')?.value||'M1'} · LIVE`;setStatus(`Live market · ${symbol} · ${new Date().toLocaleTimeString()}`);}catch(error){setStatus(`Market stream unavailable: ${error.message}`,true);}finally{busy=false;}}
  function wireControls(){$('#account')?.addEventListener('change',()=>{updateAccountCards();syncSelectedAccount();});$('[data-symbol]')?.addEventListener('change',()=>{selectedSymbol=$('[data-symbol]').value;livePoints=[];syncLiveTick();});}
  async function boot(){if(document.body.dataset.authenticated!=='true'&&!$('#chart'))return;wireControls();await syncAccounts();await discoverSymbol();await syncLiveTick();setInterval(()=>{if(document.visibilityState==='visible')syncLiveTick();},3000);setInterval(()=>{if(document.visibilityState==='visible')syncAccounts();},30000);}
  window.addEventListener('DOMContentLoaded',boot,{once:true});
})();
