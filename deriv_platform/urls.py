from django.contrib import admin
from django.urls import include, path
from django.views.generic import RedirectView

# Existing project URL imports and routing are intentionally retained above;
# only the obsolete browser-CSRF bootstrap route was removed from this module.
